using UnityEngine;

public class IHateGit : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("I really hate Git!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
